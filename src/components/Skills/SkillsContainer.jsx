import React from "react";
import "./skillStyle.css";

const SkillsContainer = ({ children }) => {
  return <section className="column skills">{children}</section>;
};

export default SkillsContainer;
